# PoolKiosk backend — Raspberry Pi runtime requirements ONLY.
# Keep this minimal — every add here means an extra pip install on the Pi.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
aiosqlite>=0.20.0
pydantic>=2.9.0
python-dotenv>=1.0.0
paho-mqtt>=2.1.0
httpx>=0.27.0
# When the Modbus/RS485 phase ships, add: pymodbus>=3.7.0, pyserial>=3.5
